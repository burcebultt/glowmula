
  # Combine design into website

  This is a code bundle for Combine design into website. The original project is available at https://www.figma.com/design/bGSpchYzqJ9b6cC8asZRNh/Combine-design-into-website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  